# Leave empty. The React app is served from the Express server on the same origin
# (http://localhost:3000), so /api calls go to that same host.
#
# Set this only if the React app is hosted on a different origin than the API.
VITE_API_URL=
